<% if @question_count.nil?%>
   window.location.replace("<%= condition_path(current_user) %>");
<% else %>
   $('#container-single').html("<%= escape_javascript(render('conditions/form'))%>");
<% end %>
